export const API_BASE_URL = "http://localhost:8080/api/users";

export const UPLOAD_URL = "http://localhost:8080/api/documents";

export const ATTACHMENT_URL = "http://localhost:8080/api/attachments";

export const LOGIN_ENDPOINT = `${API_BASE_URL}/login`;

export const REGISTER_ENDPOINT = `${API_BASE_URL}/register`;

export const FILE_UPLOAD_ENDPOINT = `${UPLOAD_URL}/upload/excel`;

export const REPORT_ENDPOINT = `${ATTACHMENT_URL}/attachments`

export const DOWNLOAD_ENDPOINT = `${ATTACHMENT_URL}/download`